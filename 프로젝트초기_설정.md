# 프로젝트 초기 설정 가이드 (Day 1)

**자신의 GitHub 저장소를 새로 만들어서** Local LLM 앱 실습을 시작할 때 따라가는 순서입니다.  

---

## 0. 준비물 체크리스트

시작 전 아래 계정/프로그램이 준비되어 있는지 확인합니다.

- [ ] GitHub 계정
- [ ] Git 설치
- [ ] Python 3.12 이상 설치
- [ ] Node.js 18 이상 설치
- [ ] [Ollama](https://ollama.com/download) 설치
- [ ] 코드 에디터 (VSCode 권장)

### 설치 확인 명령어

```bash
git --version
python --version      # 또는 python3 --version
node --version
npm --version
ollama --version
```

버전이 안 뜨면 해당 프로그램을 먼저 설치합니다.

---

## 1. Git 최초 설정 (컴퓨터마다 딱 1번만)

Git을 처음 설치했다면 "누가 커밋했는지" 알려주는 사용자 정보를 등록해야 합니다.

```bash
git config --global user.name "본인 이름 또는 GitHub 아이디"
git config --global user.email "GitHub에 등록한 이메일"
```

확인:

```bash
git config --global --list
```

### GitHub 인증 준비

- HTTPS 방식: `git push` 시 GitHub 로그인 창(또는 Personal Access Token)이 뜨면 진행
- (선택) SSH 방식을 쓰려면 `ssh-keygen`으로 키를 만들고 GitHub 설정에 공개키 등록

---

## 2. GitHub에 저장소 만들기

1. GitHub에 로그인 → **New repository** 클릭
2. 저장소 이름 예: `local_llm_app2th`
3. **Public/Private** 선택 (실습용이면 Private 추천)
4. **"Add a README file" 체크하지 않음** (로컬에서 만들 것이므로)
5. **Create repository** 클릭 후, 안내되는 저장소 URL을 복사해 둡니다.
   예: `https://github.com/아이디/local_llm_app2th.git`

---

## 3. 로컬 프로젝트 폴더 생성 및 Git 연동

```bash
# 1) 프로젝트 폴더 생성 및 이동
mkdir local_llm_app2th
cd local_llm_app2th

# 2) Git 저장소 초기화
git init

# 3) 기본 브랜치 이름을 main으로 설정 (버전에 따라 이미 main일 수 있음)
git branch -M main

# 4) GitHub 원격 저장소 연결
git remote add origin https://github.com/아이디/local_llm_app2th.git

# 5) 연결 확인
git remote -v
```

### .gitignore 먼저 준비

첫 커밋 전에 `.gitignore`부터 만들어 두어야 `.venv`, `node_modules` 같은 대용량/불필요 파일이 커밋되지 않습니다.

```gitignore
# Python
.venv/
__pycache__/
*.pyc
.env

# uv
uv.lock

# Node
node_modules/
dist/

# OS / 에디터
.DS_Store
.vscode/
```

> `uv.lock`은 협업 시 버전 고정을 위해 커밋하는 경우도 많습니다. 팀 방침에 따라 제외 여부를 정하세요.

### 첫 커밋 & 푸시

```bash
git add .
git commit -m "chore: 프로젝트 초기 설정"
git push -u origin main
```

이후부터는 `git push`만 입력해도 `origin main`으로 푸시됩니다.

---

## 4. Python 개발 환경 설정 (uv)

uv 설치가 안 되어 있다면 먼저 설치합니다.

```bash
# macOS / Linux / WSL
curl -LsSf https://astral.sh/uv/install.sh | sh

# Windows (PowerShell)
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

프로젝트 폴더(방금 만든 저장소)에서 uv 프로젝트를 초기화합니다.

```bash
uv init --bare --python 3.12 --name local_llm_app
```

필요한 패키지를 추가합니다 (예: FastAPI + Ollama 연동).

```bash
uv add "fastapi[standard]" ollama requests
```

실행은 `uv run`으로 합니다.

```bash
uv run python main.py
```

> 폴더별로 필요한 패키지가 다를 수 있습니다. 자세한 패키지 목록은 [README.md](README.md)를 참고하세요.

변경 사항을 커밋합니다.

```bash
git add pyproject.toml uv.lock
git commit -m "chore: uv 프로젝트 초기화"
git push
```

---

## 5. Ollama 모델 준비

```bash
# Ollama 실행 확인
sudo systemctl start ollama   <- linux
systemctl status ollama       <- linux

# 실습에 사용할 모델 다운로드
ollama pull exaone3.5:7.8b
```

---

## 6. (필요 시) 프론트엔드 환경 설정 (Node.js)

React 등 프론트엔드를 함께 만든다면:

```bash
npm create vite@latest frontend -- --template react
cd frontend
npm install
git add .
git commit -m "chore: 프론트엔드(Vite+React) 초기화"
git push
```

---

## 7. 작업 규칙 (간단 버전)

처음 시작하는 실습이므로 복잡한 브랜치 전략 없이 아래 규칙만 지킵니다.

- 기능 하나를 완성할 때마다 **작은 단위로 커밋**한다.
- 커밋 메시지는 `기능: 설명` 형식 예: `기능: 채팅 API 연동`, `수정: CORS 오류 해결`
- 실습 중간중간 `git push`로 GitHub에 백업한다.
- 코드가 깨진 상태로 하루를 마치지 않는다 (돌아가는 상태에서 커밋).

---

## 8. 최종 체크리스트

- [ ] `git --version`, `python --version`, `node --version`, `ollama --version` 모두 정상 출력
- [ ] `git config --global user.name/email` 설정 완료
- [ ] GitHub에 개인 저장소 생성
- [ ] 로컬 폴더에서 `git init` → `remote add origin` → 첫 `push` 완료
- [ ] `.gitignore`에 `.venv/`, `node_modules/` 포함 확인
- [ ] `uv init` + `uv add`로 Python 환경 구성 완료
- [ ] `ollama pull`로 모델 다운로드 완료
- [ ] GitHub 저장소 페이지에서 커밋이 정상적으로 보이는지 확인
