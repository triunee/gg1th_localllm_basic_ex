# nginx 테스트 및 배포
# 1. React + Nginx 배포
## 1) React build
```
cd ~/local_llm_app/frontend
npm run build
ls -al dist
```

## 2) Ubuntu에 nginx 설치 및 상태 확인
- 설치
```
sudo apt update
sudo apt install -y nginx
nginx -v
```

- 테스트
```
sudo service nginx start
sudo service nginx status

# 포트 충돌 확인
sudo ss -tulpen | grep nginx

```
## 3) React build 파일을 Nginx에 복사하기
- React build 파일 복사하기
```
# 기존 페이지 삭제
sudo rm -f /etc/nginx/sites-enabled/default
sudo rm -f /var/www/html/index.html
sudo rm -f /var/www/html/index.nginx-debian.html

# 배포 폴더 생성
sudo mkdir -p /var/www/local_llm_app

# (주)재 배포시 폴더 파일 정리
sudo rm -rf /var/www/local_llm_app/*

# 폴더 확인하기
ls -al /var/www/local_llm_app

# dist 파일 복사하기
cd ~/local_llm_app/frontend
sudo cp -r dist/* /var/www/local_llm_app/
ls -al /var/www/local_llm_app               # 복사한 결과 확인
```
- 권한 설정 : Nginx 프로세스가 정적 파일을 읽을 수 있도록 권한 설정하기
```
sudo chown -R www-data:www-data /var/www/local_llm_app
sudo chmod -R 755 /var/www/local_llm_app
```

## 4) Nginx server block 작성
### 설정 파일 생성 및 작성
- shell 에서 명령 입력
```
sudo nano /etc/nginx/sites-available/local_llm_app
```
- 아래 내용을 복사해서 붙여 넣기
```
server {
    listen 80;
    listen [::]:80;

    server_name localhost;

    root /var/www/local_llm_app;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000/;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 300;
        proxy_connect_timeout 300;
        proxy_send_timeout 300;
    }
}
```
- 저장 방법:  Ctrl + O → Enter → Ctrl + X
### 설정 활성화하기
- Nginx에서는 보통 설정 파일을 두 위치로 나누어 관리한다.
- 서버 하나에서 여러 웹사이트를 안전하게 관리하기 위해서 분리해서 관리를 한다.

```
# Nginx에서 사용할 사이트 설정 파일을 활성화하는 명령을 실행한다.
sudo ln -s /etc/nginx/sites-available/local_llm_app /etc/nginx/sites-enabled/local_llm_app

# 기본 사이트 설정과의 충돌을 방지하기 위해, Nginx의 default 사이트를 비활성화한다.
sudo rm -f /etc/nginx/sites-enabled/default
```

## 5) Nginx 웹서버 실행
```
sudo systemctl restart nginx
sudo systemctl reload nginx
sudo systemctl status nginx
```

# 2. fastapi 배포
```
cd ~/local_llm_app/backend
# forground 실행
uv run main.py --host 127.0.0.1 --port 8000

# background 실행
nohup uv run main.py --host 127.0.0.1 --port 8000 > app.log 2>&1 &

# 프로세스 확인
ps -ef | grep uv

# uv fastapi 프로세스 죽이기
pkill  -9 uv

```

# 3. 배포후 외부에서 접속하기
- 접속 컴퓨터는 반드시 동일한 네트워크에서 테스트를 해야합니다.
- 방화벽 열고, WSL 포트 포워딩하는 명령입니다.
- **관리자 PowerShell (윈도우)**에서 실행하세요.
```
$wslIp = (wsl hostname -I).Trim().Split()[0]

New-NetFirewallRule `
  -DisplayName "WSL Nginx HTTP 80" `
  -Direction Inbound `
  -Action Allow `
  -Protocol TCP `
  -LocalPort 80

netsh interface portproxy add v4tov4 `
  listenaddress=0.0.0.0 `
  listenport=80 `
  connectaddress=$wslIp `
  connectport=80
```

- powershell에서 IP 확인하는 법
```
ipconfig
```

- 외부 컴퓨터 또는 핸드폰 브라우저에서 확인하기
```
http://윈도우ip주소
```